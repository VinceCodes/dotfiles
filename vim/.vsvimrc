" Remaps the leader key to espace.
let mapleader=" "

" Some shortcut that allows to refresh the VIMRC config file.
:nnoremap <leader>r :source %UserProfile%\.vimrc<CR>

" Go to definition, to behave as I would expect it does.
:nnoremap gd :vsc Edit.GoToDefinition<CR>

" Saves the current file.
:nnoremap <leader>w :w<CR>

" Saves all files.
:nnoremap <leader>wa :vsc File.SaveAll<CR>

" Build all: usually control shift+B.
:nnoremap <leader>b :vsc Build.BuildSolution<CR>

" Sets autoindent.
set autoindent

" Allows to move individual lines up and down quite easily.
nnoremap <A-j> :m .+1<CR>==
nnoremap <A-k> :m .-2<CR>==

" Shows the number column
:set number